/**
 * MarkdownPlugin.tsx
 * ------------------
 * Lexical plugin to handle Markdown parsing and serialization.
 * Converts editor content to Markdown and can initialize editor
 * state from a Markdown string.
 */

export const MarkdownPlugin = () => {
  return <div>MarkdownPlugin</div>;
};
