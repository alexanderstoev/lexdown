/**
 * LexDownRenderer.tsx
 * -------------------
 * Optional component for rendering Markdown outside of the editor.
 * Converts Markdown strings into styled JSX/HTML using the same
 * parsing logic as LexDown, allowing consistent display.
 */


export const LexDownRenderer = () => {
  return (
    <div>LexDownRenderer</div>
  )
}
